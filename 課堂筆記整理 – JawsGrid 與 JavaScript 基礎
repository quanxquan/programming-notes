# JavaScript 與 JawsGrid 課程筆記整理(約2500字)

## 一、課程概述與學習目標

本課程進度安排較為緊湊,內容涵蓋面廣。主要學習目標包括:理解JawsGrid的基本概念與應用、掌握網頁互動操作技巧、熟悉基本程式語法結構、了解常用資料結構,以及安裝配置Dino開發工具以便日常練習。課程強調實作與理論並重,鼓勵學生透過大量練習鞏固所學知識。

## 二、JawsGrid 核心概念

### 2.1 什麼是 JawsGrid

JawsGrid 是實現網頁互動效果的重要工具,能夠控制網頁元素的顯示與隱藏、調整字體大小、改變顏色等視覺效果。其核心原理是透過程式控制元素的顯示狀態來創造動態效果。

### 2.2 基本控制方法

在 JawsGrid 中,我們可以控制多種網頁元素屬性:

**字體與顏色控制**: 可以動態調整文字的大小、顏色、字型等屬性,實現豐富的視覺變化。

**顯示與隱藏機制**: 透過設定元素的 display 屬性為 block(顯示)或 none(隱藏)來控制元素的可見性。這是實現許多互動效果的基礎,例如下拉選單、彈出視窗、圖片輪播等功能都依賴此機制。

### 2.3 程式語言選擇

課程主要使用 JavaScript 作為網頁端的互動程式語言。JavaScript 是瀏覽器原生支援的腳本語言,無需額外安裝即可運行。此外,課程也提及 Python 可用於開發 REST API(後端服務),但這屬於進階選修內容。對於網頁前端互動而言,JavaScript 是必備且最適合的選擇。

## 三、JavaScript 基礎語法

### 3.1 Console 控制台使用

開發者工具的 Console 是程式除錯的重要工具。使用 `console.log()` 函數可以將訊息輸出到控制台,方便觀察程式執行狀況。例如:
```javascript
console.log("Hello World");
console.log(123);
```

### 3.2 變數型態

JavaScript 支援多種基本資料型態:

**整數 (Integer)**: 如 1, 2, 100 等整數值。

**浮點數 (Float)**: 如 3.14, 2.5 等帶有小數點的數值。

**字串 (String)**: 用引號包圍的文字,如 "Hello" 或 'World'。

**布林值 (Boolean)**: 只有 true 和 false 兩個值,用於邏輯判斷。

JavaScript 是動態型別語言,變數在宣告時不需要指定型態,型態可以在執行時改變。

### 3.3 運算符

**邏輯運算符**:
- `&&` (and): 邏輯且
- `||` (or): 邏輯或
- `!` (not): 邏輯非

**位元運算符**:
- `&`: 位元且
- `|`: 位元或
- `~`: 位元非

邏輯運算用於條件判斷,位元運算則用於二進位層級的操作。

### 3.4 變數宣告

在 JavaScript 中宣告變數需使用關鍵字:

```javascript
let age = 25;        // 可重新賦值的變數
const name = "CCC";  // 不可重新賦值的常數
```

JavaScript 的動態型別特性允許同一個變數在不同時間儲存不同型態的值,這提供了靈活性但也需要開發者更謹慎地管理變數。

## 四、資料結構

### 4.1 陣列 (Array)

陣列是儲存多個值的有序集合,在 JavaScript 中非常常用。

**一維陣列宣告**:
```javascript
let arr1 = [];              // 空陣列
let arr2 = [1, 2, 3, 4, 5]; // 包含元素的陣列
```

**多維陣列**: JavaScript 支援在陣列中巢狀陣列,形成二維、三維甚至更高維度的資料結構:
```javascript
let matrix = [[1,2,3], [4,5,6], [7,8,9]];  // 二維陣列
let cube = [[[1,2],[3,4]], [[5,6],[7,8]]]; // 三維陣列
```

陣列可以混合儲存不同型態的元素,例如:
```javascript
let mixed = [1, "hello", true, 3.14];
```

### 4.2 字典/物件 (Dictionary/Object)

字典是鍵值對的集合,在 JavaScript 中稱為物件(Object):

```javascript
let person = {
    name: "CCC",
    age: 55,
    isTeacher: true,
    height: 175.5
};
```

存取物件屬性有兩種方式:
```javascript
console.log(person.name);     // 使用點記法
console.log(person["age"]);   // 使用括號記法
```

字典的值可以是任意型態,包括陣列或其他字典,這使得可以建構複雜的資料結構。

### 4.3 匿名函數與箭頭函數

**匿名函數**: 沒有名稱的函數,可以直接賦值給變數:
```javascript
let greet = function(name) {
    return "Hello, " + name;
};
```

**箭頭函數**: ES6 引入的簡潔語法:
```javascript
let add = (a, b) => a + b;
let square = x => x * x;
```

這些函數可以作為參數傳遞給其他函數,這在處理回呼函數和陣列方法時特別有用。

## 五、控制流程

### 5.1 迴圈結構

**for 迴圈**: 最常用的迴圈結構:
```javascript
for(let i = 0; i < 10; i++) {
    console.log(i);
}
```

**陣列遍歷**:
```javascript
let arr = [1, 2, 3, 4, 5];

// 遍歷索引
for(let i in arr) {
    console.log(i);  // 印出索引: 0, 1, 2, 3, 4
}

// 遍歷元素值
for(let value of arr) {
    console.log(value);  // 印出值: 1, 2, 3, 4, 5
}
```

**continue 與 break**:
- `continue`: 跳過本次迴圈的剩餘程式碼,直接進入下一次迭代
- `break`: 完全跳出迴圈

### 5.2 條件判斷

JavaScript 的條件判斷語法與 C 語言相似:
```javascript
if(condition) {
    // 條件為真時執行
} else if(anotherCondition) {
    // 另一條件為真時執行
} else {
    // 以上條件都不滿足時執行
}
```

也支援三元運算符:
```javascript
let result = (age >= 18) ? "成年" : "未成年";
```

### 5.3 函數作為參數

JavaScript 允許將函數作為參數傳遞給其他函數,這是函數式編程的重要特性:

```javascript
function calculate(a, b, operation) {
    return operation(a, b);
}

let sum = calculate(5, 3, (x, y) => x + y);
let product = calculate(5, 3, (x, y) => x * y);
```

這種特性使得程式碼更加靈活和可重用。課程中甚至提到可以對函數進行微分計算,展示了函數式編程的強大功能。

## 六、Dino 開發環境

### 6.1 安裝方法

**Windows 系統**: 需要使用 PowerShell 並以系統管理員權限執行安裝命令。開啟 PowerShell 的方式是在開始選單搜尋並右鍵選擇「以系統管理員身分執行」。

**Mac 系統**: 直接使用終端機(Terminal)執行安裝命令即可,大多數 Mac 電腦可能已經預裝了 Dino。

### 6.2 使用方式

安裝完成後,在命令列輸入 `dino` 即可啟動開發環境。Dino 提供了互動式的程式執行環境,可以:
- 直接輸入 JavaScript 程式碼並立即看到結果
- 測試程式邏輯和語法
- 進行快速的原型開發

### 6.3 注意事項

**防毒軟體影響**: 某些防毒軟體可能會將 Dino 的行為視為可疑並影響執行速度,必要時可以將 Dino 加入白名單。

**執行環境確認**: 使用前應確認 Dino 已正確安裝,可以透過 `dino --version` 命令檢查版本。

## 七、練習與作業指引

### 7.1 基本操作練習

建議的練習項目包括:

**變數操作**: 練習宣告不同型態的變數,進行加法、字串連接等操作。

**陣列操作**: 建立陣列、存取元素、修改元素、計算長度等。

**字典操作**: 建立物件、存取屬性、新增或刪除屬性。

**迴圈與條件**: 使用 for、while 迴圈,配合 if-else 進行邏輯判斷。

### 7.2 作業安排

課程安排了十題練習題,建議採用以下方式完成:

**檔案命名**: 每題使用獨立檔案,按照 01.js、02.js...10.js 的格式命名,便於管理和查找。

**先存檔再執行**: 養成良好習慣,程式碼寫完後先儲存,再執行測試。這樣可以避免因執行錯誤而影響其他題目的進度。

**錯誤處理**: 遇到錯誤時,善用 Console 輸出訊息幫助除錯,逐步檢查程式邏輯。

### 7.3 進階練習建議

完成基礎練習後,可以挑戰以下進階內容:

**匿名函數與箭頭函數**: 練習不同的函數寫法,理解各自的適用場景。

**多維陣列操作**: 處理二維、三維陣列,例如矩陣運算、座標系統等。

**高階函數**: 練習將函數作為參數傳遞,使用 map、filter、reduce 等陣列方法。

**實際應用**: 嘗試結合 HTML 和 CSS,製作簡單的互動網頁效果,例如按鈕點擊改變文字顏色、顯示隱藏元素等。

## 八、學習建議

**循序漸進**: JavaScript 的概念環環相扣,建議按照課程進度逐步學習,不要跳過基礎部分。

**多加練習**: 程式設計需要大量實作才能熟練,每個概念都應該親手寫過程式碼驗證。

**善用工具**: 充分利用 Console、Dino 等開發工具,它們能大幅提升學習和除錯效率。

**建立筆記**: 整理自己的學習筆記,記錄常用語法和容易犯錯的地方,方便日後複習。

**主動探索**: 除了課堂內容,可以嘗試閱讀相關文件或教學資源,拓展知識面。

透過本課程的學習,學生將建立起紮實的 JavaScript 基礎,為日後開發互動式網頁應用打下良好根基。記住,程式設計能力的提升需要時間和耐心,持續練習是成功的關鍵。
