**資料庫 (Database)** 與 **FastAPI** 結合，並引入了 **Session (對話機制)** 與 **部署 (Deployment)** 的觀念。

---

### 一、 資料庫的進化：從原生 SQLite 到 SQLAlchemy

老師展示了兩套操作資料庫的方法：

1. **原生 `sqlite3**`：
* 需要手寫 **SQL 指令**（如 `CREATE TABLE`, `INSERT INTO`, `SELECT *`）。
* **Cursor (游標)**：像是在 Excel 裡的選取框，告訴電腦現在要操作哪一行、哪一格。
* **Commit (提交)**：這是初學者最常忘記的。資料寫入時是先放在緩衝區，沒下 `commit()`，資料就不會真的存進硬碟。


2. **SQLAlchemy (ORM 技術)**：
* **ORM (Object-Relational Mapping)**：把資料庫表格對應成 Python 的 **類別 (Class)**。
* **優點**：不需要寫 SQL 字串，直接操作 Python 物件。且具備「資料庫無關性」，今天用 SQLite，明天換成 MySQL，程式碼幾乎不用改。



---

### 二、 Session：伺服器如何「記住」你是誰？

HTTP 協議本身是「無狀態」的（它不記得你上一秒是誰）。老師介紹了 **Session Middleware** 來解決這個問題：

* **登入流程**：
1. 用戶輸入帳密 -> 後端驗證成功。
2. 後端在 **Session** 裡存入 `user_id`。
3. 瀏覽器會存一個加密的小餅乾 (Cookie)，下次請求時帶上它。


* **權限檢查**：當你要「貼文」或「刪除」時，程式會先去 Session 裡面看有沒有你的資料，沒有就踢回登入頁。
* **Secret Key (密鑰)**：老師強調那個很亂的字串不能外流，否則駭客可以偽造身分。

---

### 三、 實戰技巧：IP 穿透與 NGROK

這部分解決了「我的電腦架站，別人怎麼連進來」的問題：

* **內網連線**：在校內連同一個 WiFi，直接打老師的內網 IP（如 `192.168.x.x`）就能看到網誌。
* **NGROK (內網穿透)**：老師示範了如何透過 NGROK 產生一個臨時的「公網網址」。
* **原理**：它像是在你的電腦與公網之間挖了一條隧道。
* **限制**：免費版重啟後網址會變，且流量有限。



---

### 四、 習題解析：為 Blog 加上「刪除功能」

老師要求：**加上刪除按鈕，且只有「原作者」能刪除。**

#### 實作思路 (邏輯步驟)：

1. **HTML 模板更新 (`list.html`)**：
* 在每一則貼文旁邊加一個按鈕：`<a href="/delete/{{ post.id }}">刪除</a>`。


2. **後端路由設計 (`main.py`)**：
* 使用 `@app.get("/delete/{post_id}")` 或 `@app.post`。
* **步驟 A (檢查登入)**：先看 `session.get("user")` 有沒有東西。
* **步驟 B (查詢貼文)**：去資料庫把這筆 `post_id` 抓出來。
* **步驟 C (權限核對)**：檢查 `post.owner_id` 是否等於目前登入的 `session["user_id"]`。
* **步驟 D (執行刪除)**：如果符合，執行 `db.delete(post)` 並 `db.commit()`。


3. **防呆機制**：如果不符合權限，彈出錯誤訊息或導向首頁。

---

### 五、 老師的叮嚀 (評價)

* **Pydantic 驗證**：老師提到這個套件能幫你檢查輸入的東西是不是正確的型態（例如：年齡不能是字串），這比自己寫 `if-else` 檢查要專業得多。
* **部署工具**：提到 Vercel 這種雲端平台。老師建議大家期末作業可以嘗試架上去，但要小心「Secret Key」不要推到 GitHub 上直播出去（像老師剛才那樣）。

---

