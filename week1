# 🌐 網頁設計與開發：10 節課全紀錄

本筆記用於系統化整理網頁設計課程的技術重點、環境配置與開發心得。

---

## 📅 Week 01：開發環境配置與 HTML 核心基礎

### 1. 開發環境與核心工具 (Environment)
* **GitHub**: 全球開發者交換程式碼的平台。需熟悉 `Fork`（複製專案）、`Issue`（討論版）與 `Commit`（提交變更）的基本操作。
* **VS Code (Visual Studio Code)**: 課程推薦編輯器。屬於輕量級編輯器，與重量級的 IDE（如 Visual Studio）不同。
* **W3Schools**: 線上即時教材工具，適合快速測試 HTML/CSS 小片段。
* **路徑導航 (Relative Path)**: 
    * `.` (一個點)：代表目前資料夾。
    * `..` (兩個點)：代表**回到上一層資料夾**。
    * *開發常識：* 在引用圖片或連結檔案時，必須正確計算 `..` 的層次才能準確讀取資源。

### 2. 智慧財產權與 AI 開發規範 (IPR & AI)
* **授權意識**：網路上看到的資源（圖片、文字）預設皆不可直接使用。
* **CC 授權**：如維基百科採用的 `BY-SA` 授權，使用時須標示來源並以相同方式分享。
* **法律風險**：老師提及「古阿莫」案例，強調未經授權剪輯電影內容可能面臨高額賠償與法律訴訟。
* **AI 協作**：允許使用 AI 寫程式，但應秉持誠實原則，標註使用的 AI 種類（如 ChatGPT）並建議保留/分享對話連結。

### 3. HTML 結構標籤 (HTML Structure)
HTML 是網頁的內容骨架，由成對的標籤組成：
* **文件結構**:
    * `<!DOCTYPE html>`: 宣告為 HTML5 定義。
    * `<html>`: 包含整個網頁內容。
    * `<head>`: 存放標題 (`<title>`) 與 Metadata。
    * `<body>`: 實際呈現給使用者的畫面。
* **常用內容標籤**:
    * `<h1>` - `<h6>`: 層級標題（h1 最大）。
    * `<p>`: 段落。
    * `<a href="...">`: 超連結。
    * `<img src="...">`: 插入圖片（需注意與 HTML 檔的相對路徑）。
    * `<ul>` / `<li>`: 無序清單（點點）。
    * `<ol>` / `<li>`: 有序清單（數字）。
    * `<table>`, `<tr>`, `<th>`, `<td>`: 表格的基本結構。

### 4. 色彩學與 CSS 基礎概念 (Colors & CSS)
* **色光三原色 (RGB)**:
    * 使用紅(R)、綠(G)、藍(B)混合，每種數值為 0-255。
    * **十六進位表示法**：如 `#FF0000` (純紅)、`#FFFFFF` (純白)、`#000000` (純黑)。
* **樣式初探**: 
    * CSS 用於美化 HTML。
    * 例如表格預設較醜，需透過 CSS 的 `border-collapse` 讓邊框合併，視覺上才會更精緻。

---

## 📅 Week 02：(待更新)
